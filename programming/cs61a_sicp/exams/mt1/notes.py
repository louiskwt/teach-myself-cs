# And if True, pick the last one; if false return first falsy value | Or if (left) True, pick the first one
# print inside print evaluates to None
# --> print(1, print(40), 61)  40 > 1, None, 61 (note that inner print show up first)
# --> printing a function with reture will print the returned value 
# '10' + '10' = '1010' (string concatination)
# int / str will turn things into the corresponding type
# 0, '', [] are False
# non-zero int, non-empty str like 'Swift' are true
# lambda as argument will be evaluated in the global frame (parent: global)
# 9232 // 10 will remove the last digit from any number larger than 10
# 902 % 10 > 2 remove the rest of digit
# 635321 % pow(10, 3) > 321
